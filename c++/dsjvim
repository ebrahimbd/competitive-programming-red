#include <bits/stdc++.h>

using namespace std;

struct Node{
    int key;
    Node *link;
};


Node * insert(Node *nodes, int data){
    Node *node=new Node();
    if (node==NULL){
        return node;
    }else{
        node->key=data;
        node->link=insert(nodes, data);
        return nodes;
    }
return node;
};

void  print(Node *node){
    cout<<node->key<<endl;
}



int main(){
    cout<<"sdljkfldkf";
}
#include <bits/stdc++.h>

using namespace std;

struct Node{
    int key;
    Node *link;
};


Node * insert(Node *nodes, int data){
    Node *node=new Node();
    if (node==NULL){
        return node;
    }else{
        node->key=data;
        node->link=insert(nodes, data);
        return nodes;
    }
return node;
};

void  print(Node *node){
    cout<<node->key<<endl;
}



int main(){
    cout<<"sdljkfldkf";
}
